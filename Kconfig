# TODO Remove CTR_SDK and ensure that everything still works
menuconfig CTR_SDK
	bool "CTR_SDK"
	select ASSERT
	select DEBUG
	select POLL

rsource "Kconfig.chester_x0d"
rsource "Kconfig.defaults"
rsource "Kconfig.log"
rsource "Kconfig.shell"

rsource "drivers/Kconfig"
rsource "lib/Kconfig"
rsource "subsys/Kconfig"
