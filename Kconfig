# Todo: Remove and ensure that other subsystems still work
menuconfig CTR_SDK
	bool "CTR_SDK"
	select ASSERT
	select DEBUG
	select POLL

if CTR_SDK

config CTR_CHESTER_X3
	bool "CTR_CHESTER_X3"

endif # CTR_SDK

rsource "Kconfig.button"
rsource "Kconfig.chester_x0d"
rsource "Kconfig.config"
rsource "Kconfig.defaults"
rsource "Kconfig.log"
rsource "Kconfig.rtc"
rsource "Kconfig.shell"
rsource "Kconfig.test"

rsource "drivers/Kconfig"
rsource "lib/Kconfig"
rsource "subsys/Kconfig"
