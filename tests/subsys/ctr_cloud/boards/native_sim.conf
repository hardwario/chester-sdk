CONFIG_FLASH_SIMULATOR=y
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y

# native_sim has a CSPRNG-capable entropy driver, so the upstream Mbed TLS
# PSA backend works without the external-RNG fallback.
CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_PSA_CRYPTO_C=y
CONFIG_ENTROPY_GENERATOR=y
