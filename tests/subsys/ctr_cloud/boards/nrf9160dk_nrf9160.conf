CONFIG_FLASH=y
CONFIG_FLASH_MAP=y

# nRF targets use the nRF Security PSA backend (CryptoCell / Oberon).
# On Nordic ARM, PSA_CRYPTO selects CONFIG_PSA_CRYPTO_PROVIDER_CUSTOM, which
# auto-enables NRF_SECURITY and MBEDTLS_PSA_CRYPTO_C.
CONFIG_PSA_CRYPTO=y
