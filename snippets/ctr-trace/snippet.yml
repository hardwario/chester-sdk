name: ctr-trace
boards:
  # Regex (delimited by '/') matched against "${BOARD}${BOARD_QUALIFIERS}",
  # e.g. "chester/nrf52840".
  /chester.*/:
    append:
      EXTRA_CONF_FILE: ctr-trace.conf
      EXTRA_DTC_OVERLAY_FILE: ctr-trace.overlay
